import { useEffect, useRef, useState, useMemo } from "react";

//hook
export const useDataLoadingManagement = (data) => {
  const [visData, setVisData] = useState(null);
  
  useEffect(() => {
    
  }, [contentsWidth, contentsHeight, nodesData.length, arrangeBy])
  
  return { 
    visData
  }
};