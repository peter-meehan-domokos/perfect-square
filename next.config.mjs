/** @type {import('next').NextConfig} */
const nextConfig = {
    /*basePath:"./perfect-square",*/
    output: "export",
    reactStrictMode: false,
    images: {
        unoptimized: true
    }
};

export default nextConfig;
